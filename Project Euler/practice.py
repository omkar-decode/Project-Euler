import time
start_time = time.clock()

#print ("Execution time: %.4f" %(time.clock() - start_time)) + " sec"


from langdetect import detect

##print detect("War doesn't show who is right, only who is left")

def checkEnglish (
